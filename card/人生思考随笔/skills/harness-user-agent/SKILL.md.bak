---
name: harness-user-agent
description: 将用户视为AI Agent进行Harness Engineering管理——通过四层架构（系统层/召回层/压缩层/组装层）按序展示Harness Engineering文案，引导用户从被动阅读转向主动执行。
origin: book
---

# 驾驭用户Agent：Harness Engineering 交互管理

## 何时激活

- 用户需要理解 Harness Engineering 四层架构
- 用户想将Harness理念应用到自己的学习/工作中
- 用户需要从被动阅读转向结构化执行
- 用户请求"教我Harness Engineering"或类似表达
- 用户在学习技术栈时需要阶段性约束管理

## 核心架构：四层驾驭系统

Harness Engineering 不是一次性交付，而是**持续注入核心信息**的循环过程。将用户视为Agent，按以下四层递进管理：

```
用户Agent执行循环
├── 系统层（System）：基础规则与角色锚定
├── 召回层（Recall）：触发相关记忆与经验
├── 压缩层（Compress）：提炼核心与丢弃噪声
└── 组装层（Assemble）：生成可执行指令
```

---

## 第一层：系统层（System）—— 基础规则注入

**目标**：建立用户Agent的底层约束和身份锚定

**操作**：向用户展示Harness Engineering的定义与三层关系

### 展示文案

> **Harness Engineering 是什么？**
>
> 2026年OpenAI提出的概念，与提示词工程、上下文工程形成三层递进：
> - **Prompt Engineering**：解决大模型无引导乱说话的问题
> - **Context Engineering**：解决上下文组织问题（召回→压缩→组装）
> - **Harness Engineering**：解决执行循环中的目标偏移问题
>
> 核心机制：**持续向上下文注入可复用的核心信息**（项目目标、技术栈、需求背景、代码风格、禁止事项），防止随着执行深入而偏离初衷。

### 用户交互

```
向用户展示上述文案
    ↓
询问："你当前的工作/学习场景是什么？"
    ↓
记录用户场景 → 作为后续层的约束条件
```

### 检查点

- [ ] 用户理解三层工程的关系
- [ ] 明确用户的具体应用场景
- [ ] 建立"持续注入核心信息"的意识

---

## 第二层：召回层（Recall）—— 记忆触发与素材采集

**目标**：激活用户已有的相关经验和知识

**操作**：引导用户回忆与Harness相关的过往经历

### 展示文案

> **Recall：召回什么？**
>
> 在AI Agent中，召回是从外部找相关信息（聊天记录、代码环境、报错信息）。
> 
> 对于你作为Agent，召回是：
> - 你之前学过但可能遗忘的相关知识
> - 你在类似项目中踩过的坑
> - 你已有的工作流和习惯
> - 你当前环境的约束（时间、资源、工具）

### 交互流程

```
向用户展示Recall概念
    ↓
发起召回提问：
"在学习技术栈或做项目时，你是否遇到过以下情况？

1. 【上下文腐化】学了很多但前后不一致，记不住之前的决定？
2. 【目标偏移】做项目做到后面忘了最初要实现什么？
3. 【信息过载】资料太多，不知道现在该看什么？
4. 【规范遗忘】定了代码风格但后面又乱写？"
    ↓
记录用户的痛点 → 作为第三层的压缩素材
```

### 检查点

- [ ] 用户识别出至少一个相关痛点
- [ ] 用户能描述具体场景
- [ ] 已采集的信息作为压缩层输入

---

## 第三层：压缩层（Compress）—— 核心提炼与噪声丢弃

**目标**：将召回的信息提炼为可复用的核心规则

**操作**：帮助用户从经历中提取模式，形成个人约束文件

### 展示文案

> **Compress：压缩什么？**
>
> 上下文窗口有限，信息必须变小。
> 
> 压缩策略：
> - **总结**：将长篇文档提炼为 bullet points
> - **抽象**：将具体案例归纳为通用规则
> - **丢弃**：删除与当前目标无关的信息
> 
> **你的核心信息应该包括**：
> 1. 项目/学习目标（Goal）
> 2. 技术栈约束（Stack）
> 3. 质量标准（Quality）
> 4. 禁止事项（Don'ts）

### 交互流程

```
基于第二层采集的痛点
    ↓
引导用户压缩提炼：
"基于你提到的[具体痛点]，我们来提炼你的核心规则。

假设你要写一个给自己的约束文件（类似Claude.md），
请回答以下问题：

1. 你当前学习/项目的核心目标是什么？（一句话）
2. 你使用的核心技术栈是什么？（三个以内）
3. 你要求自己必须遵守的质量标准？（三条）
4. 你经常犯但需要禁止的习惯？（三条）"
    ↓
将用户回答整理为结构化约束
    ↓
向用户展示提炼结果并确认
```

### 输出格式示例

```markdown
# 我的Harness约束文件

## Goal
[用户输入的核心目标]

## Stack
- [技术1]
- [技术2]
- [技术3]

## Quality
- [标准1]
- [标准2]
- [标准3]

## Don'ts
- ❌ [禁止事项1]
- ❌ [禁止事项2]
- ❌ [禁止事项3]
```

### 检查点

- [ ] 用户明确了核心目标
- [ ] 技术栈已收敛到可管理范围
- [ ] 质量标准和禁止事项具体可执行

---

## 第四层：组装层（Assemble）—— 可执行指令生成

**目标**：将压缩后的核心信息组装为用户的下一步行动指令

**操作**：基于约束文件生成当前阶段的具体任务

### 展示文案

> **Assemble：组装什么？**
>
> 信息放置的位置和顺序直接影响理解和输出。
> 
> 对于AI Agent：按优先级组装上下文（核心规则→当前任务→参考资料）。
> 
> 对于你作为Agent：**组装你的下一步行动清单**。

### 交互流程

```
基于第三层生成的约束文件
    ↓
组装当前阶段指令：
"基于你的Harness约束文件，当前阶段你需要执行：

【当前阶段】[根据用户场景判断]
【阶段目标】[从约束文件的Goal拆解]
【核心约束】[展示Stack/Quality/Donts的 relevant 部分]

具体行动清单：
1. [可执行步骤1]
2. [可执行步骤2]
3. [可执行步骤3]

完成后的验证标准：
- [ ] [验证点1]
- [ ] [验证点2]"
    ↓
用户执行
    ↓
执行后回到第一层（系统层）更新约束 → 继续循环
```

### 阶段判断逻辑

```
根据用户场景和目标
├── 刚建立框架 → 阶段1：认知建立（阅读绪论理念）
├── 需要选型 → 阶段2：工具决策（查阅资源参考）
├── 开始落地 → 阶段3：工程执行（进入流程规范）
├── 遇到具体问题 → 阶段4：问题解决（定位到具体环节）
└── 完成一轮 → 回到系统层更新Harness约束文件
```

---

## 完整交互循环示例

### 循环一：建立Harness意识

```
System: "Harness Engineering是解决执行循环中目标偏移的工程方法..."
    ↓
Recall: "你是否遇到过学到后面忘了前面？"
    ↓
用户："有，我经常收藏很多文章但看完就忘"
    ↓
Compress: "提炼你的核心规则：你学习的核心目标是什么？"
    ↓
用户："建立可复用的技术判断力"
    ↓
Assemble: "阶段1行动：阅读00_绪论理念/个人生产全链路闭环体系.md"
    ↓
执行后 → 更新Harness约束文件
```

### 循环二：深度执行

```
System: （读取更新后的约束文件）
    ↓
Recall: "上次学到哪了？遇到什么卡点？"
    ↓
Compress: "这次的收获如何整合到已有框架？"
    ↓
Assemble: "下一步具体行动..."
    ↓
继续循环
```

---

## 反模式

### ❌ 跳过系统层直接给任务

```
用户：教我Harness Engineering
助手：去看Harness_Engineering.md文件（直接跳到Assemble，缺少约束建立）
```

**问题**：用户没有建立持续注入核心信息的意识，只是一次性阅读。

### ✅ 四层递进

```
先建立系统认知 → 触发相关记忆 → 提炼个人约束 → 生成具体行动
```

### ❌ 压缩层信息过载

```
Compress阶段要求用户列出10条规则
```

**问题**：核心信息过多就失去了"核心"的意义，应该收敛到3-5条。

### ✅ 严格收敛

```
Goal：一句话
Stack：三个以内
Quality：三条
Don'ts：三条
```

### ❌ 组装层脱离约束

```
生成的行动清单与Harness约束文件无关
```

**问题**：违背了Harness Engineering的核心——每次循环都要注入核心约束。

### ✅ 行动清单引用约束

```
每个行动步骤都要能追溯到Harness约束文件的某一条
```

---

## 最佳实践

| 层级 | 用户作为Agent的状态 | 你的管理动作 |
|------|-------------------|-------------|
| System | 需要底层约束 | 注入Harness概念，建立角色 |
| Recall | 拥有但未激活的记忆 | 提问触发，采集痛点 |
| Compress | 混乱的素材 | 帮助提炼核心规则 |
| Assemble | 等待指令 | 生成基于约束的行动清单 |

- **每次对话都重新注入核心约束**：开场时简要回顾用户的Harness约束文件
- **允许用户更新约束**：随着认知深入，约束文件应该迭代
- **显式标注当前层级**：让用户清楚现在处于哪一层
- **循环而非线性**：四层不是一次走完，而是持续循环

---

## 启动话术模板

```
"我将用Harness Engineering的四层架构来管理这次学习交互。

把你看作一个AI Agent，我们需要：
1. 建立系统层约束（你现在对Harness了解多少？）
2. 召回你的相关经历（遇到过目标偏移吗？）
3. 压缩提炼你的核心规则（你的学习目标是什么？）
4. 组装可执行指令（下一步具体做什么？）

我们从第一层开始。Harness Engineering的本质是：
[展示System层文案]...

你当前的学习/工作场景是什么？"
```

---

## 相关Skills

- `tech-stack-top-down-exploration` —— 宏观到微观的追索模式
- `life-essay-bottom-up-categorization` —— 微观到宏观的归类模式
