# 坍缩原型改造方案：基于现有材料的路径分析

## 〇、架构总览：两条认知路径在三层工具链中的交汇

三个项目不是独立的功能模块，而是"坍缩"所描述的两条人类认知路径，在技术栈的三层管道中逐级交汇。

### 认知路径 → 项目 → 坍缩模块 的完整映射

```
┌──────────────────────────────────────────────────────────────────┐
│                      两种认知路径                                  │
│                                                                   │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐│
│  │  肉身体感 · 界限体验          │  │  数据推演 · 幸存者航标       ││
│  │  亲历认知边界的理性边缘，      │  │  通过海量他者数据推测，       ││
│  │  让司空见惯的无形之物显形。    │  │  从炮灰的沉默墓地中          ││
│  │  获得肌肉记忆与神经本能。      │  │  还原幸存者的航标信号。      ││
│  └──────────────┬──────────────┘  └──────────────┬──────────────┘│
│                 │                                │                │
│                 ▼                                ▼                │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐│
│  │     cognitive-cards         │  │          card               ││
│  │     注意力机制调配工具        │  │     简化信号规则卡牌桌游     ││
│  │  绕过"设计者-执行者"死锁，    │  │  将复杂心理/社交动态提炼      ││
│  │  用环境条件和身体仪式         │  │  为精简的卡牌规则与信号系统。 ││
│  │  激活状态，让改变自然发生。    │  │                             ││
│  └──────────────┬──────────────┘  └──────────────┬──────────────┘│
│                 │                                │                │
│                 │     简化信号 + 注意力线索        │                │
│                 └──────────────┬─────────────────┘                │
│                                ▼                                  │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                    coze-studio                               │  │
│  │                 沉浸润色工作流编排                             │  │
│  │  以两条路径的产物为输入，通过 AI 工作流编排，                   │  │
│  │  将其润色为具象、沉浸的叙事与交互体验。                         │  │
│  │  让抽象的边界直觉和幸存者信号都变得人性化、可沉浸。              │  │
│  └─────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 三层对应坍缩三模块

| 协作关系层 | 对应坍缩模块 | 职责 |
|-----------|-------------|------|
| cognitive-cards = 边界手感直觉 | 模块一：认知压力舱 | 制造微型"窒息"，让隐形边界显形 |
| card = 幸存者航标信号 | 模块二：沉默的墓地 | 从炮灰的沉默中提取隐形变量 |
| coze-studio = 沉浸润色工作流 | 模块三：双轨坍缩画布 | 两条路径的对撞——用工程确定性模拟一次没有真正濒死的界限体验 |

### 两条路径各自的"不可读"

- **边界直觉（cognitive-cards）** 是高度个人化的体感，难以传递、难以复现——内化的肌肉记忆，不是可讲授的知识。
- **幸存者航标（card）** 是极度压缩的简化信号，像路标一样精炼，但缺少血肉——它指示方向，却不营造经历。

coze-studio 的位置正是这两条路径的**交汇与代偿**：把来自 card 的简化信号"演绎"为具象的场景、对话、交互；同时把来自 cognitive-cards 的注意力线索嵌入沉浸内容中，让体验者在接触叙事的同时，被环境条件和身体仪式引导着进入状态。

> 它做的事情，本质上是用工程的确定性，去模拟一次没有真正濒死的"界限体验"——让"水"在安全的环境中显形，然后用幸存者航标指引方向。**不亲历窒息，也能看见空气。**

---

## 一、现有材料全景

`code/` 目录下有三套可以互相借力的系统：

| 系统 | 技术栈 | 当前状态 | 核心能力 |
|------|--------|----------|----------|
| **cognitive-cards** | Python/FastAPI + 原生HTML/CSS/JS | 可运行 (`python server.py`，端口8170) | 10张认知卡片 + 8枚锦囊 + 涌现引擎（输入→卡片匹配→语料精筛→引导词生成） |
| **card/** | Markdown知识库 + 多个HTML原型 + Obsidian工作流 | 静态资料 + 可启动的交互原型 | 设计者/执行者双模架构、心流闭环导航、桌游规则体系、创伤理解交互原型 |
| **coze-studio** | Go后端 + workflow引擎 | 完整项目但较重 | 工作流编排、Agent系统、插件机制 |

---

## 二、改造策略总览：三层协同扩展，而非各自重写

`cognitive-cards` 作为骨架，`card/` 的知识库作为血肉，`coze-studio` 的工作流作为沉浸生成层。三个系统各自扩展，最终在"坍缩"前端汇合。

```
cognitive-cards（现有服务）                    card（知识库）
    │                                            │
    ├── 扩展 ──▶ 模块一：认知压力舱               ├── 内容源 ──▶ 模块二的 failure-corpus.json
    │            压力场景数据 + 会话API            │             模块一的 pressure-scenarios.json
    │                                            │
    ├── 扩展 ──▶ 模块二：沉默的墓地               │
    │            失败案例库 + 盲区检测API          │
    │                                            │
    └── 扩展 ──▶ 模块三：双轨坍缩画布 ────────────┘
                 碰撞管道API         coze-studio（可选进阶）
                                       │
                                       └── 沉浸润色：将简化信号 + 注意力线索
                                           演绎为具象交互场景（Phase 3 接入）
```

---

## 三、模块一：认知压力舱 → 从"桌游规则 + 场景文档"改出

> 对应协作关系中的 **cognitive-cards 注意力机制调配工具**：用环境条件和身体仪式激活状态，让改变自然发生。

### 3.1 现有可复用的材料

| 材料 | 位置 | 可转换的内容 |
|------|------|-------------|
| 能量流通桌游规则 | `card/桌游/能量流通：桌游规则设计.md` | 六阶段循环（规划→执行→修复→复盘）+ 淤塞惩罚机制 → 压力场景的骨架 |
| 桌游特殊机制 | `card/桌游/认知觉醒资源/特殊机制/` | Burnout破产、暴露/隐藏双状态、噪音破窗惩罚 → 压力触发事件 |
| 心流闭环导航 | `card/人生思考随笔/心智模拟/心流闭环导航/` | 迷失预警、负向沉沦、自防御后、回归执行者 → 压力舱的状态流转 |
| 四阶段八场景 | `card/人生思考随笔/心智模拟/应用界面/` | 8个具象场景（深度探索的迷宫、西西弗斯的苦役、多巴胺沼泽等）→ 直接作为压力剧本 |
| 常识问答链 | `card/人生思考随笔/心智模拟/应用界面/常识问答链_骨架_11条常识.md` | 可作为压垮前的"理性防线崩溃检测" |

### 3.2 改造路径

**第一步：定义压力场景数据模型**

在 `cognitive-cards/data/` 下新增 `pressure-scenarios.json`，结构如下：

```json
{
  "scenarios": [
    {
      "id": "pressure_01",
      "name": "西西弗斯的苦役",
      "type": "liminal",           // liminal | collapse | recovery
      "intensity": 7,              // 1-10
      "stages": [
        {
          "stage": 1,
          "name": "初始舒适区",
          "description": "你感觉到一切尽在掌控...",
          "hidden_blindspot": "节奏的外部依赖",
          "trigger_event": { "type": "timer", "value": 90, "unit": "seconds" }
        },
        {
          "stage": 2,
          "name": "压力递增",
          "description": "第一波干扰袭来...",
          "hidden_blindspot": "过度反应的模式识别",
          "trigger_event": { "type": "interrupt", "count": 3 }
        },
        {
          "stage": 3,
          "name": "临界点",
          "description": "你感到旧有的应对模式开始失效...",
          "hidden_blindspot": "尚未命名的隐形假设",
          "trigger_event": { "type": "breakpoint" }
        },
        {
          "stage": 4,
          "name": "破局时刻",
          "description": "旧模型在冲击中碎裂。暂停，凝视这个瞬间。",
          "capture_prompt": "在你崩溃的那一刻，什么'理所当然'的东西第一次变得可疑？"
        }
      ]
    }
  ]
}
```

**第二步：扩展 server.py**

在 `cognitive-cards/server.py` 中新增 API：

```python
# POST /api/pressure-scenario/start   → 初始化一个压力场景会话
# GET  /api/pressure-scenario/{session_id}/next-stage → 推进到下一阶段
# POST /api/pressure-scenario/{session_id}/reflect → 用户提交破局时刻反思
```

**第三步：前端——新增一个独立页面**

复用 `cognitive-cards/static/` 的结构，创建 `static/pressure.html`。核心交互：

- 全屏沉浸式界面（无导航栏，只有倒计时 + 情境文本）
- 每个阶段有限时决策（类似文字冒险游戏）
- 到达"临界点"时，强行暂停，展示"破局时刻捕捉"输入框
- 用户提交后，生成一份"隐形边界显形记录"

### 3.3 核心体验闭环

```
选择剧本 → 进入舒适区 → 压力递增（定时/随机事件） → 临界点崩溃 
→ 暂停捕捉（关键） → 反思提交 → 生成"体感记录"
```

---

## 四、模块二：沉默的墓地 → 从"语料库 + 专题展开"改出

> 对应协作关系中的 **card 简化信号规则卡牌桌游**：将海量他者的代价提炼为幸存者航标信号。真正重要的弹孔在那些没能飞回来的飞机上。

### 4.1 现有可复用的材料

| 材料 | 位置 | 可转换的内容 |
|------|------|-------------|
| 语料库系统 | `cognitive-cards/data/corpus.json` + `server.py` 语料精筛逻辑 | 已有匹配、评分、关键词对齐管道，直接改为"失败案例库" |
| 幸存者偏差相关文章 | `card/核心内容文档/已串联文档/` | 破窗效应与受害者困局、变异临界点、伤口展示困局 |
| 社会系统底层规则 | `card/人生思考随笔/社会系统底层规则集·元胞自动机模型.md` | 可作为"反向盲区检测"的逻辑基础 |
| 投资潜力而非抚慰苦难 | `card/人生思考随笔/投资潜力，而非抚慰苦难.md` | 失败分析的叙事模板 |

### 4.2 改造路径

**第一步：新增失败案例数据模型**

在 `cognitive-cards/data/` 下新增 `failure-corpus.json`：

```json
{
  "entries": [
    {
      "id": "fail_01",
      "domain": "认知与决策",
      "case_name": "过度准备的悖论",
      "survivor_narrative": "那些'做足准备再开始'的人最终都开始了",
      "hidden_variable": "准备行为本身正在消耗启动所需的心理能量——这种消耗不可见，因为只有放弃的人才会察觉",
      "why_invisible": "成功者的叙事中，准备前的犹豫被事后合理化为'深思熟虑'；失败者从未有机会讲述",
      "blindspot_type": "时间不对称盲区",
      "related_cards": ["card_01", "card_05"],
      "symptoms": ["反复修改计划", "总觉得信息不够", "在'研究'中消耗主要精力"],
      "counter_narrative": "任何执行者状态下的5分钟，其信息密度超过设计者状态下5小时的'准备'"
    }
  ]
}
```

**第二步：新增 API**

```python
# GET  /api/failure-corpus → 获取所有失败案例
# POST /api/blindspot-check → 用户提交当前计划，系统对照失败案例库，返回潜在盲区
# GET  /api/failure-corpus/{domain}/patterns → 按领域聚合的失败模式
```

盲区检测的核心逻辑（已有 EmergenceEngine 可复用）：

```
用户提交当前构想
    │
    ▼
关键词提取（复用 _extract_keywords）→ 匹配失败案例库的 symptoms
    │
    ▼
返回匹配案例的 hidden_variable + why_invisible
    │
    ▼
排序：按 blindspot_type 聚类，每个类型输出最高风险的一个案例
```

**第三步：前端**

在认知卡片前端中新增"盲区检测"入口——用户在卡片页输入一段自己的计划/构想，系统从失败者墓地中提取匹配的隐形变量，以"沉默的警示"形式返回。

### 4.3 关键设计原则

- 不是"告诉你别做什么" —— 是 "告诉你有哪些东西在你的视野之外"
- 每条案例的核心不是"死因"，而是 "当时不可见的变量"
- 同一盲区类型只呈现一次，避免信息轰炸
- 正如幸存者偏差的核心教训：采集到的永远是抵达边缘但没有牺牲的少数——真正致命的，在沉默中

---

## 五、模块三：双轨坍缩画布 → 从"设计者/执行者双轨"改出，coze-studio 做沉浸润色

> 对应协作关系中的 **coze-studio 沉浸润色工作流**：两条路径在此交汇——来自 card 的简化信号被演绎为具象场景，来自 cognitive-cards 的注意力线索嵌入沉浸内容。让抽象的边界直觉和幸存者信号都变得人性化、可沉浸。

### 5.1 现有可复用的材料

| 材料 | 位置 | 可转换的内容 |
|------|------|-------------|
| 设计者/执行者双模架构 | `cognitive-cards/设计理念总览.md` + 心流闭环 | 天然的双轨结构——设计者=数据推演轨道，执行者=肉身体感轨道 |
| 核心循环 | `设计者锚点 → 执行者具身 → 设计者复盘 → 外沿扩大` | 直接改写为：数据轨道 → 体感轨道 → 碰撞 → 坍缩 |
| 锦囊路径系统 | `cognitive-cards/data/kits.json` (8枚锦囊) | 锦囊A-H对应不同的碰撞阶段引导 |
| 涌现引擎 | `cognitive-cards/server.py` EmergenceEngine | 三阶段管道可直接复用为碰撞管道的骨架 |
| coze-studio workflow | `code/coze-studio/` 工作流引擎 | Phase 3 接入：将碰撞结果润色为沉浸叙事（进阶） |

### 5.2 改造路径

**第一步：新增碰撞数据模型**

```python
class CollapseRequest(BaseModel):
    user_intuition: str    # 用户的直觉判断（体感轨道输入）
    plan_context: str      # 用户的当前计划/构想

class CollapseResponse(BaseModel):
    # 体感轨道
    matched_cards: list       # 用户直觉命中了哪些认知卡片
    intuition_analysis: dict  # 体感轨道的分析

    # 数据轨道
    blindspots: list          # 失败者墓地的盲区检测结果
    statistical_context: dict # 相关失败模式的统计摘要

    # 碰撞结果
    tension_points: list      # 体感与数据的张力点
    collapse_question: str    # 坍缩后的核心追问
    guidance: str             # 引导词（复用 EmergenceKit 格式）
```

**第二步：碰撞管道**

```
用户输入（你的直觉 + 你的计划）
        │
        ├──▶ 体感轨道（cognitive-cards 路径）：
        │     ┌─ 语义匹配 → 认知卡片（复用 match_cards）
        │     └─ 输出：你的直觉属于哪类认知模式
        │
        ├──▶ 数据轨道（card 路径）：
        │     ┌─ 盲区检测 → 失败案例库（复用 blindspot-check）
        │     └─ 输出：历史上此类直觉的失败模型
        │
        ▼
    碰撞引擎：
    ┌─ 对比两条轨道的结论
    ├─ 找出张力点（直觉说冲，数据说停 / 直觉说停，数据显示停滞）
    ├─ 生成坍缩追问（非二选一，而是揭示矛盾的本质）
    └─ 输出引导词
```

**第三步：前端——坍缩画布**

在 `cognitive-cards/static/` 下新增 `collapse.html`，核心视觉：

- 左轨道：暖金色，展示"你的体感告诉你的东西"（复用卡片展示组件）
- 右轨道：冷蓝色，展示"失败者墓地说的话"（盲区卡片）
- 中央碰撞区：玫紫色，两条轨道的内容在此对撞，输出一个"坍缩追问"
- 视觉上复用了之前 `坍缩.html` 中的碰撞动效（双光球 + 脉冲中心点）

**第四步（进阶）：coze-studio 沉浸润色管道接入**

当碰撞管道产生张力点和坍缩追问后，可接入 coze-studio 的工作流：

```
碰撞管道输出
    │
    ├── 简化信号（盲区检测结果）──▶ coze-studio 演绎为具象场景/对话
    │
    ├── 注意力线索（卡片匹配 + 引导词）──▶ coze-studio 嵌入沉浸环境
    │
    └── 坍缩追问 ──▶ coze-studio 包装为交互叙事节点
```

这一步本质上是用 AI 工作流编排，把来自 card 的"路标"变成"可走的路"，把来自 cognitive-cards 的"体感"变成"可进入的状态"。

### 5.3 一个具体的碰撞示例

> **用户输入**：我直觉上想等这个项目完全想清楚了再开始。数据上我知道拖延不好，但我就是觉得还差一点。

| 轨道 | 来源 | 输出 |
|------|------|------|
| 体感轨道 | cognitive-cards | 命中 Card 01"二阶观察者困境"——你的设计者心态正在用"想清楚"来逃避执行者状态下的不确定性 |
| 数据轨道 | card（失败者墓地） | 失败案例 #fail_01"过度准备的悖论"——可观测到的数据中，'完全想清楚再开始'人群的表观成功率不低，但沉默的样本是那些永远没有开始的人 |

> **坍缩追问**：你是在"想清楚"，还是在用"想清楚"的行为本身来获得一种"正在做事"的体感，而不需要面对真正执行中那种失控的滋味？

---

## 六、分阶段实施计划

### Phase 1：核心管道打通

只改 `cognitive-cards/`，不动 `card/` 的知识库结构：

1. 新增 `data/pressure-scenarios.json`（从`card/桌游/` 场景转化，3-5个场景）
2. 新增 `data/failure-corpus.json`（从`card/核心内容文档/` 转化，10-15条初始案例）
3. 在 `server.py` 中新增：
   - `/api/pressure-scenario/*` 三个端点
   - `/api/blindspot-check` 盲区检测
   - `/api/collapse` 碰撞管道
4. 新增前端页面 `static/pressure.html` 和 `static/collapse.html`

### Phase 2：内容填充（持续）

将 `card/` 中的丰富素材逐步转化为结构化数据：

- 8个场景 → 8个压力剧本（`pressure-scenarios.json`）
- 专题展开的13个Topic → 按领域归入失败案例库
- 设计者视角的22条信念库条目 → `failure-corpus.json` 的案例源

### Phase 3：双轨坍缩前端 + coze-studio 润色接入

在 `static/collapse.html` 中实现完整的双轨画布，包含：
- 左右双轨滚动展示
- 中间碰撞可视动画
- 引导词生成后的流动过渡
- **可选**：碰撞结果通过 coze-studio 工作流润色为沉浸叙事场景

---

## 七、与现有系统的共存关系

```
cognitive-cards（保持不变）
    │
    ├── 原有功能：卡片浏览、锦囊查询、涌现引擎
    │   访问：http://localhost:8170/
    │
    ├── 新增：pressure.html（认知压力舱）
    │   访问：http://localhost:8170/pressure
    │
    ├── 新增：collapse.html（双轨坍缩画布）
    │   访问：http://localhost:8170/collapse
    │
    └── 碰撞结果 ──▶ coze-studio workflow ──▶ 沉浸润色产物（Phase 3）
```

- **不破坏现有功能**：原有卡片系统继续运行
- **共享数据层**：卡片数据、语料库、锦囊被三个模块共用
- **新增独立的JSON数据文件**：`failure-corpus.json`、`pressure-scenarios.json`
- **前端新增独立页面**：不影响现有 `static/index.html`
- **coze-studio 为可选加速器**：即使不接入，Phase 1-2 的碰撞管道已可独立运行

---

## 八、从纸质材料到数字化产品的关键转换

`card/` 中的材料本质上是**个人的外化思考**——它们是写给自己的。转换为产品原型时，需要做以下转换：

| 原材料特质 | 转换为 |
|------------|--------|
| 第一人称自述式的理论 | 第二人称引导式的体验（"你正处于..."） |
| 完整的理论框架 | 按需浮现的碎片化洞察（在用户触发时才展开） |
| 抽象的概念命名 | 具象的情境触发 + 感官锚点 |
| 长篇论述 | 短脉冲式的"破局时刻"捕捉 |
| 静态文档 | 状态机驱动的交互流程 |

**认知卡片系统的锦囊（Kits）** 已经完成了这个转换——它们就是用第二人称、感官锚点、短脉冲式引导写的。这条路径可以照搬到压力和碰撞模块。

---

## 九、完整流向：从代价到路标，从濒死到看见空气

```
                他者的代价            个体的边界
              (沉默的墓地)          (界限体验·濒死)
                   │                      │
                   ▼                      ▼
              ┌──────────┐          ┌──────────────┐
              │   card   │          │cognitive-    │
              │ 幸存者   │          │cards         │
              │ 航标信号  │          │边界手感直觉   │
              └────┬─────┘          └──────┬───────┘
                   │                       │
                   │  简化信号              │  注意力线索
                   │                       │
                   └───────────┬───────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │    coze-studio      │
                    │   沉浸润色工作流      │
                    │   （坍缩碰撞引擎）    │
                    │                     │
                    │  信号 → 具象场景     │
                    │  直觉 → 沉浸环境     │
                    │  路标 → 可走的路     │
                    └─────────────────────┘
```

> 不亲历窒息，也能看见空气。
